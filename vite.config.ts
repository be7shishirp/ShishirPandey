// vite.config.ts
import { defineConfig } from 'vite'

export default defineConfig({})
// https://vitejs.dev/config/
export default defineConfig({
  base: "/ShishirPandey/",
  plugins: [react()],
  },
});
